var name = 'bboodd'
//var letter = 'As an '+name+ ' \n asynchronous event-driven JavaScript runtime, '+name+' Node.js is designed to build scalable network applications. '+name+' In the following "hello world" example, many connections can be handled concurrently. Upon each connection, the callback is fired, but if there is no work to be done, Node.js will sleep. '+name;
//console.log(letter);

var letter = `As an ${name} asynchronous event-driven JavaScript runtime, ${name} Node.


js is designed to build scalable network applications. ${name} In the following "hello world" example,
 many connections can be handled concurrently. Upon each connection, the callback is fired, but if there is no work to be done, Node.js will sleep. ${name}`;

console.log(letter);
